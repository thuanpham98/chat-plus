export enum RouterPath {
  root = "/",

  // home group
  home = "/home",
  chatWithEveryOne = `${home}/chat-with-everyone`,
  chatting = `${home}/chatting`,

  // auth group
  auth = "/auth",
  login = `${auth}/login`,
}
